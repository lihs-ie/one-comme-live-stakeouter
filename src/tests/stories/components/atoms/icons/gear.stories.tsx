import { GearIcon } from 'components/atoms/icons/gear';

import type { Meta, StoryObj } from '@storybook/react';

const meta = {
  component: GearIcon,
} satisfies Meta<typeof GearIcon>;

export default meta;

export const Default: StoryObj<typeof GearIcon> = {};
